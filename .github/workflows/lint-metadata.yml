name: Lint Repository Metadata

on:
  workflow_dispatch:
  schedule:
    # Run weekly on Mondays
    - cron: '0 0 * * 1'

jobs:
  lint-metadata:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Validate repository metadata
        run: |
          echo "📋 Repository Metadata Validation"
          echo "=================================="
          echo ""
          echo "This workflow checks repository metadata best practices."
          echo "To fully validate, ensure your repository has:"
          echo ""
          echo "✅ Repository description (under 160 characters)"
          echo "✅ 5-10 relevant topics/tags"
          echo "✅ Clear, searchable repository name"
          echo "✅ README.md with proper structure"
          echo ""
          echo "Note: Some checks require GitHub API access."
          echo "Configure GITHUB_TOKEN permissions for full validation."
